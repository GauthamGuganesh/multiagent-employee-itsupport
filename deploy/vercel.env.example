# GA-VoiceAI Vercel environment template
# This value is public and is embedded into the browser bundle at build time.
# Never put keys, passwords, database URLs, or other secrets in this file.

NEXT_PUBLIC_API_URL=https://<your-render-service>.onrender.com
